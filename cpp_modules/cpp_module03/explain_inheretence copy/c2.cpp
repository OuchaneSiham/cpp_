3. Basic Syntax of Inheritance
class Parent {
    // Parent class members
};

class Child : public Parent {
    // Child class inherits from Parent
};

The access specifier (public, protected, private) determines how members of the parent class are inherited.